# GitHub Pages demos

`.github/workflows/pages.yml` validates the project, renders the existing ingest and fulfillment static HTML demos, and deploys them with GitHub’s native Pages actions. `pages/index.html` is the lightweight landing page; this meets the need for shareable demos without adding a server, framework, or dependency.
